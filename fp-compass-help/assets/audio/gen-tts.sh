#!/bin/zsh
# FP Compass ヘルプ TTS (Neural2-C 1.15x + 誤読対策)
set -e
VOICE="ja-JP-Neural2-C"
RATE="1.15"
PROJECT="skeleton-pricer-130118"
OUT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$OUT_DIR"

# 誤読対策:
#  「開く/開き」→ 「ひらく」「ひらき」明示
#  「入る/入って」→ 「はいる」「はいって」明示
#  「一目」→ 「ひとめ」
#  「6タブ」→ 「ろく タブ」等 数字読み

declare -A NARR=(
  ["01-login"]="FPコンパスを、まいにち使いはじめる、いちばん最初の手順から、ご説明します。ブラウザで、アプリのURLにアクセスすると、ログイン画面がひらきます。ご登録のメールアドレスとパスワードを入力して、ログインボタンをおしてください。これで、あなた専用のダッシュボードに、はいります。"

  ["02-dashboard"]="ログインすると、まずダッシュボードがひらきます。ここには、きょうのZoom面談の予定が、うえから順にならんでいます。みぎがわには、今月の顧客数、完了した面談数、売上の見込みが、大きなカードで表示されます。さらに、したのほうには、お客様からとどいた新着メッセージと、期限がちかいタスクが、いちらんで確認できます。朝いちばんに、この画面をひらくだけで、きょうの動きが、すべて見えます。"

  ["03-clients"]="お客様をおさがしの場合は、ひだりメニューの、顧客タブをおえらびください。登録された全顧客が、いちらんで表示されます。うえの検索ボックスから、なまえや、メールアドレスで、しぼりこみができます。また、ステータスのタブで、アクティブ、休眠、解約、といった分類も可能です。気になるお客様のなまえをタップすると、詳細のカルテ画面がひらきます。"

  ["04-modal"]="お客様をえらぶと、詳細のカルテ画面がひらきます。うえには、6つのタブがならんでいます。概要タブでは、基本情報と、今こまっているポイント、そして、事前アンケートの結果が確認できます。LINEタブには、これまでのメッセージのやりとりが、すべて残っています。人生年表タブでは、進学や、退職などのライフイベントが、時系列でならびます。面談録タブでは、過去の議事録を、いつでも見返せます。質疑応答タブと家族タブでは、過去の質問と、ご家族の情報が確認できます。"

  ["05-survey"]="事前アンケートの結果は、顧客カルテの、概要タブの、したはんぶんに表示されます。年代、職業、家族構成、年収、住居、資産、そして、相談したいテーマや、いちばん不安なことまで、全部で13問の回答が、いちらんでならびます。面談がはじまる前に、この内容を確認しておくだけで、アイスブレイクが、ぐっとスムーズになります。"

  ["06-line"]="お客様にLINEでメッセージをおくるときは、顧客カルテの、LINEタブをひらきます。過去のやりとりが、うえにならんでいます。いちばんしたの入力らんをタップして、おくりたい内容を入力してください。入力しおわったら、みぎがわの送信ボタンをおします。数秒で、お客様のLINEにとどきます。議事録の共有や、次回の候補日提案も、ここからおこなえます。"

  ["07-recording"]="面談の録音と、AI議事録の使い方を、最初から最後まで、ご説明します。まず、画面ひだりのサイドバーにある、「急きょ面談スタート」をおします。入力画面がひらいたら、うえのリストから、面談するお客様をえらびます。つぎに、面談スタイルを、3つの中からえらびます。Zoomで面談する場合は、いちばんうえ、対面録音は、まんなか、電話や、訪問先で、メモだけ残す場合は、いちばんしたです。えらんだら、したの、「えらんだスタイルで開始」をおします。Zoomをえらぶと、お客様のLINEに、URLが自動でとどき、そのまま面談をはじめられます。対面をえらぶと、初回のみ、マイクの使用きょかがでますので、きょかをおしてください。面談がおわったら、Zoomをとじるか、対面の場合は、ていしボタンをおします。画面のみぎうえに、「議事録生成中」の通知がでて、30秒から1分で完成します。完成したら、顧客いちらんから、該当のお客様をひらき、じょうぶタブの、ひだりから3ばんめ、「面談録」をおします。いちばんうえに、新しい議事録カードがならぶので、タップすると、プロフィール、課題、提案、数字、次回アクション、合意事項の、6セクションと、FPがつぎにやるタスク、そして、次回面談の提案まで、まとめて表示されます。タスクは、チェックで完了、LINE下書きも自動でついてきます。質疑応答タブには、お客様がつぎにききそうな質問も、AIが予測してならびます。"

  ["08-timeline"]="お客様のライフイベントは、顧客カルテの、人生年表タブで確認できます。過去から未来まで、時系列でならびます。進学、退職、結婚、出産など、重要なイベントが、いちらんになります。議事録からAIが自動でちゅうしゅつしたイベントも、ここに反映されます。新しいイベントをついかする場合は、みぎうえの、「ついか」ボタンをお使いください。"

  ["09-meetings"]="過去の面談記録は、顧客カルテの、面談録タブで確認できます。日付順に、カード形式でならびます。カードをタップすると、議事録の全文、当時のタスク、次回提案などが、すべてひらいて表示されます。過去の提案や、やくそくしたことを確認したいとき、ここをひらけば、すぐにわかります。"

  ["10-zoom"]="次回の日程をとるときは、顧客カルテの、LINEタブから、候補日提案きのうをお使いください。提案ボタンをおして、候補を3つえらびます。たとえば、来週火曜日の14時、水曜日の10時、木曜日の19時、などです。送信すると、お客様のLINEに、3つの候補がとどきます。お客様がひとつえらぶと、自動で確定され、ZoomのURLも発行され、カレンダーにも登録されます。"

  ["11-calendar"]="Googleカレンダーとの連携は、設定画面からおこなえます。初回のみ、Googleアカウントで認証してください。連携が完了すると、FPコンパスで確定したすべての予定が、Googleカレンダーに自動で同期されます。また、カレンダー側で直接変更した場合も、FPコンパス側に反映されるので、二重管理の心配がありません。"

  ["12-liff"]="お客様が見ている、LINE内の画面を確認するきのうです。リフとよびます。お客様は、LINEのリッチメニューから、この画面をひらきます。そこには、事前アンケート、議事録の確認、次回予約、質問投稿などのメニューがならびます。FP側から、お客様にどう見えているかを確認する場合は、設定から、プレビューモードをお使いください。"
)

gen() {
  local n="$1" text="$2"
  printf "  [%s] %4d文字 → " "$n" "${#text}"
  python3 -c "
import json
print(json.dumps({
    'input': {'text': '''$text'''},
    'voice': {'languageCode': 'ja-JP', 'name': '$VOICE'},
    'audioConfig': {'audioEncoding': 'MP3', 'speakingRate': $RATE, 'sampleRateHertz': 44100}
}, ensure_ascii=False))" > /tmp/p.json
  TOKEN=$(gcloud auth print-access-token)
  resp=$(curl -s -X POST -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json; charset=utf-8" \
    -H "x-goog-user-project: $PROJECT" \
    -d @/tmp/p.json \
    "https://texttospeech.googleapis.com/v1/text:synthesize")
  echo "$resp" | python3 -c "
import json, sys, base64
d = json.load(sys.stdin)
if 'audioContent' in d:
    open('$n.mp3','wb').write(base64.b64decode(d['audioContent']))
    print('✓')
else:
    print('✗ ' + str(d.get('error',{}).get('message','?'))[:120])
    sys.exit(1)"
}

ONLY="${1:-}"
for n in $(echo "${(k)NARR[@]}" | tr ' ' '\n' | sort); do
  if [ -n "$ONLY" ] && [[ "$n" != *"$ONLY"* ]]; then continue; fi
  gen "$n" "${NARR[$n]}"
done

echo ""
for f in *.mp3; do
  dur=$(ffprobe -i "$f" -show_entries format=duration -v quiet -of csv="p=0" 2>/dev/null)
  printf "  %s: %.1fs\n" "$f" "$dur"
done
