#!/bin/zsh
# FP Compass ヘルプ ナレーション TTS bulk (Google Cloud Neural2-C 1.20x)
set -e
VOICE="ja-JP-Neural2-C"
RATE="1.20"
PROJECT="skeleton-pricer-130118"
OUT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$OUT_DIR"

declare -A NARR=(
  ["01-login"]="朝、 FP コンパス を 開きます。 ブラウザ で、 アプリ の URL に アクセス してください。 続いて、 ご登録 の メールアドレス と パスワード を 入力 します。 最後 に、 ログイン ボタン を 押すと、 あなた専用 の ダッシュボード が 開きます。"

  ["02-dashboard"]="ログイン すると、 まず ダッシュボード が 開きます。 ここ では、 今日 の Zoom 面談 の 予定 が、 上 から 順 に 並んで います。 右側 には、 今月 の 顧客 数、 完了 した 面談 数、 売上 の 見込み が、 大きな カード で 表示 されます。 さらに 下 には、 お客様 から 届いた 新着 メッセージ と、 期限 が 近い タスク が、 一覧 で 確認 できます。 朝 一番 に この 画面 を 開く だけ で、 今日 の 動き が 全部 見えます。"

  ["03-clients"]="お客様 を お探し の 場合 は、 左メニュー の 顧客 タブ を お選び ください。 登録 された 全 顧客 が、 一覧 で 表示 されます。 上 の 検索 ボックス で、 名前 や、 メールアドレス で 絞り込み が できます。 また、 ステータス の タブ で、 アクティブ、 休眠、 解約 など の 分類 も 可能 です。 気になる お客様 の 名前 を タップ する と、 詳細 の カルテ 画面 が 開きます。"

  ["04-modal"]="お客様 を 選ぶ と、 詳細 の カルテ 画面 が 開きます。 上 に 6 つ の タブ が 並んで います。 概要 タブ で は、 基本 情報 と、 今 困って いる ポイント、 そして 事前 アンケート の 結果 が 確認 できます。 LINE タブ で は、 これまで の メッセージ の やり取り が 全部 残って います。 人生 年表 タブ で は、 進学 や 退職 など の ライフ イベント が、 時系列 で 並びます。 面談録 タブ で は、 過去 の 議事録 を いつでも 見返せます。 質疑応答 タブ と、 家族 タブ で は、 過去 の 質問 と、 ご家族 の 情報 が 確認 できます。"

  ["05-survey"]="事前 アンケート の 結果 は、 顧客 カルテ の 概要 タブ の 下半分 に 表示 されます。 年代、 職業、 家族 構成、 年収、 住居、 資産、 そして 相談 したい テーマ や、 一番 不安 な こと まで、 全部 で 13 問 の 回答 が、 一覧 で 並びます。 面談 が 始まる 前 に この 内容 を 確認 して おく だけ で、 アイス ブレイク が ぐっと スムーズ に なります。"

  ["06-line"]="お客様 に LINE で メッセージ を 送る 時 は、 顧客 カルテ の LINE タブ を 開きます。 過去 の やり取り が 上 に 並んで います。 一番 下 の 入力 欄 を タップ して、 送り たい 内容 を 入力 して ください。 入力 し 終わったら、 右側 の 送信 ボタン を 押し ます。 数 秒 で、 お客様 の LINE に 届きます。 議事録 の 共有 や、 次回 の 候補 日 提案 も、 ここ から 行え ます。"

  ["07-recording"]="ここ から が、 FP コンパス の 一番 の 特徴 です。 面談 の 録音 と、 AI に よる 議事録 の 自動 生成 です。 面談 が 始まる 時、 顧客 カルテ の 右下 に ある、 赤い 録音 ボタン を 押し ます。 マイク の アクセス を 許可 する と、 録音 が 始まります。 そのまま 普通 に お客様 と お話し ください。 面談 が 終わったら、 もう 一度 録音 ボタン を 押して 停止 します。 すると、 約 30 秒 から 1 分 で、 AI が 議事録 を 自動 で 作って くれます。 議事録 の 内容 は、 顧客 プロフィール、 課題、 提案 内容、 議論 した 数字、 次回 まで の アクション、 そして 合意 事項 の 6 セクション で 構成 されます。 さらに、 FP が 次 に やる べき タスク、 次回 面談 の 提案、 お客様 が 質問 して きそう な こと、 そして 新しい ライフ イベント の 候補 まで、 全部 自動 で 出て きます。"

  ["08-timeline"]="お客様 の ライフ イベント は、 顧客 カルテ の 人生 年表 タブ で 確認 できます。 過去 から 未来 まで、 時系列 で 並びます。 進学、 退職、 結婚、 出産 など、 重要 な イベント が 一覧 に なります。 議事録 から AI が 自動 で 抽出 した イベント も、 ここ に 反映 されます。 新しい イベント を 追加 する 場合 は、 右上 の 追加 ボタン を お使い ください。"

  ["09-meetings"]="過去 の 面談 記録 は、 顧客 カルテ の 面談録 タブ で 確認 できます。 日付 順 に カード 形式 で 並びます。 カード を タップ する と、 議事録 全文、 当時 の タスク、 次回 提案 など が、 全部 開いて 表示 されます。 過去 の 提案 や、 約束 した こと を 確認 したい 時、 ここ を 開けば すぐ に わかります。"

  ["10-zoom"]="次回 の 日程 を 取る 時 は、 顧客 カルテ の LINE タブ から、 候補 日 提案 機能 を お使い ください。 提案 ボタン を 押し、 候補 を 3 つ 選び ます。 たとえば、 来週 火曜 の 14 時、 水曜 の 10 時、 木曜 の 19 時 など です。 送信 する と、 お客様 の LINE に、 3 つ の 候補 が 届きます。 お客様 が 1 つ 選ぶ と、 自動 で 確定 され、 Zoom の URL も 発行 され、 カレンダー に も 登録 されます。"

  ["11-calendar"]="Google カレンダー と の 連携 は、 設定 画面 から 行え ます。 初回 のみ、 Google アカウント で 認証 して ください。 連携 が 完了 する と、 FP コンパス で 確定 した 全て の 予定 が、 Google カレンダー に 自動 で 同期 されます。 また、 カレンダー 側 で 直接 変更 した 場合 も、 FP コンパス 側 に 反映 される ので、 二重 管理 の 心配 が ありません。"

  ["12-liff"]="お客様 が 見て いる、 LINE 内 の 画面 を 確認 する 機能 です。 リフ、 と 呼びます。 お客様 は、 LINE の リッチ メニュー から、 この 画面 を 開きます。 そこ には、 事前 アンケート、 議事録 の 確認、 次回 予約、 質問 投稿 など の メニュー が 並びます。 FP 側 から、 お客様 に どう 見えて いる か を 確認 する 場合 は、 設定 から プレビュー モード を お使い ください。"
)

gen() {
  local n="$1" text="$2"
  printf "  [%s] %4d文字 → " "$n" "${#text}"
  python3 -c "
import json
print(json.dumps({
    'input': {'text': '''$text'''},
    'voice': {'languageCode': 'ja-JP', 'name': '$VOICE'},
    'audioConfig': {'audioEncoding': 'MP3', 'speakingRate': $RATE, 'sampleRateHertz': 44100}
}, ensure_ascii=False))" > /tmp/p.json
  TOKEN=$(gcloud auth print-access-token)
  resp=$(curl -s -X POST -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json; charset=utf-8" \
    -H "x-goog-user-project: $PROJECT" \
    -d @/tmp/p.json \
    "https://texttospeech.googleapis.com/v1/text:synthesize")
  echo "$resp" | python3 -c "
import json, sys, base64
d = json.load(sys.stdin)
if 'audioContent' in d:
    open('$n.mp3','wb').write(base64.b64decode(d['audioContent']))
    print('✓')
else:
    print('✗ ' + str(d.get('error',{}).get('message','?'))[:120])
    sys.exit(1)"
}

ONLY="${1:-}"
for n in $(echo "${(k)NARR[@]}" | tr ' ' '\n' | sort); do
  if [ -n "$ONLY" ] && [[ "$n" != *"$ONLY"* ]]; then continue; fi
  gen "$n" "${NARR[$n]}"
done

echo ""
for f in *.mp3; do
  dur=$(ffprobe -i "$f" -show_entries format=duration -v quiet -of csv="p=0" 2>/dev/null)
  printf "  %s: %.1fs\n" "$f" "$dur"
done
