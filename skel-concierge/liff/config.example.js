// LIFF page runtime config.
// Copy this file to config.js (which is git-ignored, or committed if the values are safe to expose).
// Values are attached to window so index.html can pick them up before liff.init().
window.__LIFF_ID__ = 'YOUR-LIFF-ID';                    // LINE Developer Console で 発行 された LIFF ID
window.__API_URL__ = 'https://script.google.com/macros/s/AKfycb.../exec';  // GAS Web App URL
